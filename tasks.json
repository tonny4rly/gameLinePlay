{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build GTK App",
            "type": "shell",
            "command": "gcc",
            "args": ["-o", "${workspaceFolder}/code2.O", "${workspaceFolder}/code2.0.c", "`pkg-config --cflags --libs gtk+-3.0`"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
